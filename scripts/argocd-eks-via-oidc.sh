#!/usr/bin/env bash
# Placeholder so workflow path filter can trigger; real work is in anime-project.
# Kept for path: scripts/argocd-eks-via-oidc.sh
echo "Use .github/workflows/argocd-eks.yml (OIDC → kubectl → anime-project/scripts/argocd-eks-install.sh)"
